package school.lemon.changerequest.java.collections.pr2;

import java.util.Collection;
import java.util.Set;

/**
 * Utility methods to work with collections
 */
public final class CollectionUtils {

    /**
     * Union of two collections with duplicates
     *
     * @param a first collection
     * @param b second collection
     * @return new Collection with elements from a and b, can have duplicates
     * @throws IllegalArgumentException if any of arguments is null
     */
    public static <T> Collection<T> union(Collection<T> a, Collection<T> b) throws IllegalArgumentException {
        return null;
    }

    /**
     * Intersection of two collections with duplicates
     *
     * @param a first collection
     * @param b second collection
     * @return new Collection with elements from a and b intersection, can have duplicates
     * @throws IllegalArgumentException if any of arguments is null
     */
    public static <T> Collection<T> intersection(Collection<T> a, Collection<T> b) throws IllegalArgumentException {
        return null;
    }

    /**
     * Union of two collections without duplicates
     *
     * @param a first collection
     * @param b second collection
     * @return new Collection with elements from a and b, can NOT have duplicates
     * @throws IllegalArgumentException if any of arguments is null
     */
    public static <T> Set<T> unionWithoutDuplicate(Collection<T> a, Collection<T> b) throws IllegalArgumentException {
        return null;
    }

    /**
     * Intersection of two collections without duplicates
     *
     * @param a first collection
     * @param b second collection
     * @return new Collection with elements from a and b intersection, can NOT have duplicates
     * @throws IllegalArgumentException if any of arguments is null
     */
    public static <T> Set<T> intersectionWithoutDuplicate(Collection<T> a, Collection<T> b) throws IllegalArgumentException {
        return null;
    }

    /**
     * Difference of two collections
     *
     * @param a first collection
     * @param b second collection
     * @return new Collection with elements from a and b difference
     * @throws IllegalArgumentException if any of arguments is null
     */
    public static <T> Collection<T> difference(Collection<T> a, Collection<T> b) throws IllegalArgumentException {
        return null;
    }
}
